# spring boot는 test의 경우 자동으로 in-memory모드로 돌아가게 되어있다.
spring:
#  datasource:
#    url: jdbc:h2:mem:test  # in-memory mode
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true -> System.out 으로 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
